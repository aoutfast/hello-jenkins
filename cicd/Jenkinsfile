pipeline {
  agent{
      kubernetes {
        yamlFile 'cicd/pods.yml'
      }
  }
  stages {
    stage("Test") {
      steps {
          container('node-runner') {
            sh "npm install"
            sh "npm test"
          }
      }
    }
  }
  
}
